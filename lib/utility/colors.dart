import 'dart:ui';
import 'package:flutter/material.dart';
/*This class is used to define the color codes
* And can be use anywhere in application*/
class AppColors {
  static const colorLightBlack = Color.fromRGBO(0, 0, 0, 0.2);
  static const colorBlue = Color(0xFF1464F6);
  static const colorRed = Color(0xFFfb4a4d);
}
